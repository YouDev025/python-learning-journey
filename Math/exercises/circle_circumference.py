# Ask for radius and calculate:
# circumference = 2 * math.pi * r
import math

radius = float(input("Enter a radius: "))
circumference = 2 * math.pi * radius
print(f"The circumference of circle is {circumference:0.2f}")