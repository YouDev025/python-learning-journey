# Ask for a decimal number and round it UP using math.ceil()
import math

decimal = float(input("Enter a decimal number: "))
rounded_up = math.ceil(decimal)
print(f"The rounded number {decimal} is {rounded_up}")
