# Ask for a decimal number and round it DOWN using math.floor()
import math

decimal = float(input("Enter a decimal number: "))
rounded_down = math.floor(decimal)
print(f"The rounded number {decimal} is {rounded_down}")